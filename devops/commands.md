# PM2

to Start from CLI ```pm2 start pnpm --name klimat22 -- start```
pm2 logs
pm2 flush to clear logs

pm2 start pnpm --name "next-app" -- start -- -i max

sudo adduser deploy
sudo usermod -aG sudo deploy
su deploy
https://nodesource.com/products/distributions systemwide better for pm2
intsall npm via apt 
install pnpm via npm
git clone https://<username>:<token>@github.com/<username>/<repo.git>
pnpm install
pnpm exec convex dev
pnpm build
working!
pnpm setup
pnpm install -g pm2@latest
working!
create ecosystem.config.js
created 2 instances
created upstream in nginx sites-enabled
nginx -t
systemctl reload nginx

ecosystem example
pm2 reload app_name
